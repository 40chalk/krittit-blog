import React from 'react'
import classes from './hero.module.css'

function Hero() {
  return (
    <div className={classes.hero}>
      Banner (names of popular code languages image)
    </div>
  )
}

export default Hero
